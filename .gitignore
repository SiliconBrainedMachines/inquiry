.dev/*
!.dev/runbooks

.inquiry/
.ape/

# Inquiry — cleanroom versioning opt-in (this repo tracks cleanrooms as research artifacts)
!cleanrooms/

# Inquiry — cycle-local runtime state is derived, never tracked
cleanrooms/**/.iq.state.yaml

# Inquiry — CLI benchmark scratch logs are derived, never tracked
code/cli/tmp/

# spec-kit tooling (regenerable via `specify init`; design artifacts under specs/ ARE tracked)
# Exception: the Inquiry Constitution is a tracked project artifact (referenced by plans + CHANGELOG).
.specify/*
!.specify/memory/
.specify/memory/*
!.specify/memory/constitution.md
.claude/skills/speckit-*/
.github/agents/speckit.*
.github/prompts/speckit.*
# Inquiry — local workspace & cycle state
docs/requisitions/
