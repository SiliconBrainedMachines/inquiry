/// Single source of truth for Inquiry CLI version.
///
/// Keep this value in sync with pubspec.yaml and the website version badge.
/// All version surfaces are updated manually to avoid build-time complexity.
const String inquiryVersion = '0.7.2';
