name: inquiry_cli
description: >
  CLI for Inquiry — structured development through the APE methodology.
version: 0.26.1

environment:
  sdk: ^3.8.1
dependencies:
  cli_router: ^0.1.0
  datajack: ^0.1.0
  modular_cli_sdk: ^0.5.0
  skillwire: ^0.2.0
  path: ^1.9.1
  yaml: ^3.1.3
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
